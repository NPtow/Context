---
title: Session 2026-04-08
type: session
status: active
updated_at: 2026-04-08
---

# Summary
- Переосмыслен старый проект `referalka`.
- Зафиксирован переход в сторону AI-recruiting модели.
- Новый AI-recruiting контур отделен в отдельный venture `jjforrussia`.
- Старая `referalka` возвращена как отдельный historical venture про подтвержденные рефералы.
- Определен пилотный сегмент product managers.
- Сформулирована главная метрика пилота: 1 успешная оплата.
- Спроектирована LLM-first memory system и команда `обновись`.
- Добавлен детальный разбор российских конкурентов и sharpened positioning для `jjforrussia`.

# Что изменилось
- вместо обсуждения только продукта появился отдельный слой долгой памяти;
- контекст теперь должен жить в отдельном репозитории;
- принято решение хранить founder-memory отдельно от project memory.
- один смешанный project context был разделен на два venture-контейнера.
- карта рынка уточнена: найдены прямые, полупрямые, инфраструктурные и AI-native конкуренты.

# New patterns about Nikita
- Никита явно хочет, чтобы память обновлялась по короткой команде и сама понимала, что именно трогать.
- Ему важен естественный язык команд и отсутствие лишнего ceremony.

# Next
- создать структуру репозитория;
- описать команды и retrieval;
- занести seed-контент по `referalka`;
- проверить retrieval path;
- запушить в GitHub.

# Result
- начальная версия memory-системы собрана и запушена в `NPtow/Context`;
- команда `обновись` и retrieval-логика описаны в системных файлах;
- память о Никите получила первый seed-контент;
- старый `referalka` и новый `jjforrussia` получили раздельные canonical state и backlog.
- по `jjforrussia` зафиксировано, что `Getmatch` — ближайший структурный аналог, `careerspace` — сильнейший candidate-side конкурент, `hh.ru + Talantix` — системный конкурент по рынку и бюджетам.

# Live update run
- команда `обновись` была использована на живом контексте после initial push;
- стало ясно, что команда должна поддерживать не только venture updates, но и system-level updates самого `Context`;
- для такого сценария нужно обновлять `docs/status.md`, session log и при необходимости `00_system/*`, не трогая venture canonical без причины.

# Task layer
- добавлен общий блок задач `09_tasks`;
- задачи теперь имеют отдельный формализованный слой, а не достаются косвенно из project canonical;
- добавлены команды `поставить задачу`, `все задачи по проекту X`, `какие задачи сейчас есть`.

# Venture split
- `referalka` теперь означает старую линию про подтвержденный реферал в конкретную компанию;
- `jjforrussia` теперь означает новый AI-recruiting venture по мотивам Jack & Jill;
- active venture в `context-map.md` переключен на `jjforrussia`.

# Task clarification
- active task-stream уточнен: в global active index теперь только `jjforrussia` и system-level задачи;
- для `jjforrussia` абстрактные задачи разложены в более операционный pilot backlog;
- `referalka` переведена в parked backlog и больше не смешивается с текущими задачами.

# Meeting layer
- добавлен отдельный слой `10_meetings` для сохранения встреч и созвонов;
- для локальной работы создан skill `meeting-transcriber` с транскрибацией через Deepgram;
- встреча должна сохраняться как краткая note с привязкой к проекту, а не как полный transcript dump в canonical.

# Task IDs
- backlog `jjforrussia` уточнен до полного списка `JJF-001` ... `JJF-013`;
- у каждой задачи теперь есть стабильный идентификатор для привязки будущих документов и артефактов;
- в `task-schema` добавлено правило, как ссылаться на `Task ID` из новых файлов.

# Task viewer service
- после live-запроса по задачам собран отдельный viewer для `Context` task-layer;
- viewer вынесен в отдельную ветку `context/task-viewer` и отдельный worktree, чтобы не смешивать его с грязным состоянием продуктового репозитория;
- сервис читает `09_tasks/projects/*.md` напрямую из публичного GitHub repo `NPtow/Context`, а не из локального пути;
- интерфейс показывает глобальную сводку, фильтры по `status` и `project`, а также `Definition of done` по каждой задаче;
- viewer задеплоен как standalone Vercel preview: `https://context-viewer-2vb89m5z6-nptows-projects.vercel.app`;
- валидация подтвердила live-срез `22` задач по `3` проектам (`2 done`, `20 open`).

# Competitive landscape
- в project context добавлен более детальный market readout по российскому HR-tech:
- прямые: `Getmatch`, частично `careerspace`;
- полупрямые: `БУДУ`, экосистема `hh`;
- инфраструктурные: `Talantix`, `Skillaz`, `Potok`, `FriendWork`, `HURMA`, `VCV`;
- стратегические AI-риски: `Naimee AI`, `Nanimai`, `Наймус`.
- главный вывод: сильного публичного 1-в-1 аналога у `jjforrussia` пока не видно, но без чёткой verbal differentiation проект легко спутают с curated job service или career support.

# ICP and interview design
- по `jjforrussia` зафиксирован `ICP v1` как первый рабочий фильтр по target candidate, target company и anti-ICP;
- создан отдельный project artifact для `JJF-001`;
- candidate interview questions уточнены как короткий questions-only draft для `JJF-003`;
- интервью не должно превращаться в длинный discovery call про все карьерные интересы кандидата;
- core interview теперь закрывает три вещи: ICP-fit, боль текущего поиска и резонанс формата `личный AI-рекрутер`;
- вопросы про желаемые роли и company preferences вынесены из интервью в отдельный survey / инструмент.
